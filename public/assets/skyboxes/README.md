# Skybox drop zone

Binary texture assets are not versioned in this repository. To preview grounded skyboxes locally, drop your own PNG files into this directory. They will be discovered automatically by the editor via the `/api/skyboxes` endpoint.
